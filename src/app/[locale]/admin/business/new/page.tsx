import AdminBusinessFormClient from '../AdminBusinessFormClient';

export const metadata = {
    title: 'Adicionar Empresa | Admin',
};

export default function AdminBusinessNewPage() {
    return <AdminBusinessFormClient />;
}
