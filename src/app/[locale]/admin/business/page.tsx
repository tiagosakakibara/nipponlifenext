import AdminBusinessListClient from './AdminBusinessListClient';

export const metadata = {
    title: 'Business Guide | Admin',
};

export default function AdminBusinessPage() {
    return <AdminBusinessListClient />;
}
