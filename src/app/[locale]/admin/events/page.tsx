import AdminEventsClient from './AdminEventsClient';

export const metadata = {
    title: 'Agenda de Eventos | Admin',
};

export default function AdminEventsPage() {
    return <AdminEventsClient />;
}
