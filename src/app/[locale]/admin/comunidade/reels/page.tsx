import AdminReelsClient from './AdminReelsClient';

export const metadata = {
    title: 'Comunidade | Reels | Admin',
};

export default function AdminReelsPage() {
    return <AdminReelsClient />;
}
